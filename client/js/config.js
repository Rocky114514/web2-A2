/**
 * config.js — client-side configuration
 * ----------------------------------------------------------------------------
 * Single source of truth for the API address. If the Express server is moved
 * to another machine or port, only this file needs to change.
 */
const API_BASE = 'http://localhost:3000';
